namespace Scripts
{
    public enum SceneId
    {
        None = 0,
        Title,
        InGame,
    }

    public enum UILevel
    {
        HUD = 0, 
        Popup = 1,
        Overlay = 2,
    }
}